var React = require('react');
var ReactDOM = require('react-dom');

var AdjustRecipe = require('./components/app.jsx');

ReactDOM.render(
  React.createElement(AdjustRecipe),
  document.getElementById('app')
);
